# API keys are centralized at the repository root.
# Copy ../../../.env.example to ../../../.env and fill keys there.
#
# The attraction search platform reads the root .env through:
#   NEXT-STOPS/.env
